#!/bin/js
#
#  See how some bash vars work
#


echo hello world

echo "\$?=$? is not in the environment, it is internal to bash"

$.println("$? is not there: " + $.bold( $.getEnv("$?") )); // not there

echo fail && exit 5

$.println("Use errno instead: " + errno ); // "Magic" var

$.info(23, "Process ID", "" + pid); 

cat notThere & 

$.info(24, "last executed Process ID", "" + lastpid); 


