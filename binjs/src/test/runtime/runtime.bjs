#!/bin/js

#include <Runtime.js>
#include <Assert.js>

var assert = new Assert();
var runtime = new Runtime();

assert.ok(runtime.binjsVersion.length > 0);
assert.ok(runtime.bashVersion.length > 0);
assert.ok(runtime.javaScriptVersion.length > 0);

