#!/bin/js
#
# \0 is not possible to double escape into bash
#
# There are various methods in colour.js to show how to print colours
#
var i = 0;
for(; i < 128; i++) {
	$.setEnv("i", i);

	echo "\033[${i}m ${i}:colour test \033[0m"

}
